---
- hosts: all
  remote_user: student
  become: yes
  tasks:
  - name: Installing git
    yum:
       name: git
       state: latest
    become_user: root
  - name: Clone app from repo
    git:
       repo: 'git@github.com:anbogdan/webapp.git'
       dest: /home/student/app
       version: master

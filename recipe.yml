---
- hosts: all
  remote_user: student
  become: yes
  become-method: su
  tasks:
  - name: Installing git
    yum:
       name: git
       state: latest
  - name: Clone app from repo
    git:
       repo: 'https://github.com/anbogdan/webapp.git'
       dest: /home/student
       version: master
